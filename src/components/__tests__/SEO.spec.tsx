import React from 'react';
import renderer from 'react-test-renderer';

import SEO from '../SEO';

describe('<SEO/>', () => {
  it('renders correctly', () => {
    // const tree = renderer.create(<SEO title="Example site"/>).toJSON();
    // expect(tree).toMatchSnapshot();
  });
});
